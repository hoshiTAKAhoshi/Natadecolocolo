using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StageData01 : StageDataBase
{
    public StageData01() : base(
        new string[]
        {
            "SQ Q",
            " Q Q O G"
        }
    )
    {
    }

    // Start is called before the first frame update
    void Awake()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
